# AC-ONBOARDING-COMPLETE-001

## 1. Общая информация о критериях приёмки

| Name     | Description                                                     |
| -------- | --------------------------------------------------------------- |
| ID       | AC-ONBOARDING-COMPLETE-001                                      |
| Описание | критерии приёмки для функции завершения onboarding пользователя |

## 2. Таблица сценариев

| AC ID | Сценарий                                                                                                   | Given (Начальное состояние)                                                                                                                                                   | When (Действие)                                 | Then (Ожидаемый результат)                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----- | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-1  | успешное завершение onboarding с `prime_time`                                                              | пользователь существует в системе; переданы валидные `telegram`, `birth_date`, `primary_role`, `search_goal`, `prime_time`                       | система выполняет функцию завершения onboarding | система MUST провалидировать входные данные; система MUST нормализовать `telegram` в формат `t.me/...`; система MUST сохранить `telegram`, `birth_date`, `primary_role`, `search_goal`, `prime_time` в профиле пользователя; система MUST установить `is_onboarding_completed = Y`; система MUST установить `status = Active`; система MUST установить `is_market_visible` в соответствии со значением `search_goal`; система MUST вернуть `next_route = market`                              |
| AC-2  | успешное завершение onboarding без `prime_time`                                                            | пользователь существует в системе; переданы валидные `telegram`, `birth_date`, `primary_role`, `search_goal`; `prime_time` не передан            | система выполняет функцию завершения onboarding | система MUST провалидировать входные данные; система MUST нормализовать `telegram` в формат `t.me/...`; система MUST сохранить `telegram`, `birth_date`, `primary_role`, `search_goal`; система MUST установить значение `prime_time` по правилу функции; система MUST установить `is_onboarding_completed = Y`; система MUST установить `status = Active`; система MUST установить `is_market_visible` в соответствии со значением `search_goal`; система MUST вернуть `next_route = market` |
| AC-3  | публикация карточки на market при `search_goal = looking_for_team`                                         | пользователь существует в системе; переданы валидные данные onboarding; `search_goal = looking_for_team`;                                          | система выполняет функцию завершения onboarding | система MUST установить `is_market_visible = Y`; система MUST вернуть `next_route = market`                                                                                                                                                                                                                                                                                                                                                                                                   |
| AC-4  | карточка не публикуется на market при `search_goal = looking_for_players`<br>`search_goal = not_searching` | пользователь существует в системе; переданы валидные данные onboarding; `search_goal = looking_for_players`;<br>`search_goal = not_searching`;<br>`personal_data_consent = Y` | система выполняет функцию завершения onboarding | система MUST установить `is_market_visible = N`; система MUST вернуть `next_route = market`                                                                                                                                                                                                                                                                                                                                                                                                   |
| AC-5  | ошибка валидации при отсутствии обязательного поля                                                         | отсутствует хотя бы одно из обязательных полей: `telegram`, `birth_date`, `primary_role`, `search_goal`                                         | система выполняет функцию завершения onboarding | система MUST вернуть `validation_error`; система MUST NOT завершать onboarding; система MUST NOT возвращать `next_route`                                                                                                                                                                                                                                                                                                                                                                      |
| AC-6  | внутренняя ошибка выполнения функции                                                                       | пользователь существует в системе; во время выполнения произошла внутренняя ошибка                                                                                            | система выполняет функцию завершения onboarding | система MUST вернуть `internal_error`; система MUST NOT возвращать `next_route`; система MUST NOT завершать onboarding                                                                                                                                                                                                                                                                                                                                                                        |


- В критериях приемки оставлены только те сценарии, которые отражают поведение, реализуемое разработчиком.
- Клиентские ошибки, автоматические ошибки сервера и внутренние технические сбои, не требующие отдельной ручной обработки, в критерии не включены.

